name: Build macOS App

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install PyQt6 pandas openpyxl python-dotenv supabase pyinstaller

    - name: Build macOS .app
      run: |
        pyinstaller --windowed --name "BookManager" --icon=icon.icns --hidden-import=config --hidden-import=db --hidden-import=utils app.py

    - name: Upload .app
      uses: actions/upload-artifact@v4
      with:
        name: BookManager-macOS
        path: dist/BookManager.app
